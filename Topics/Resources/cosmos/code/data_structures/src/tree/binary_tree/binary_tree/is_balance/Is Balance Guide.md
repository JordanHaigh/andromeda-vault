# Is Balance

## What it does

Checks whether every node’s left and right subtree heights differ by at most one. A bottom-up traversal returns height or a failure sentinel so each node is processed once, O(n); recomputing heights at every node can cost O(n²).

## Implementations

- [is_balance.java](<https://github.com/OpenGenus/cosmos/blob/master/code/data_structures/src/tree/binary_tree/binary_tree/is_balance/is_balance.java>)

## Related topics

- [[Topics/Cosmos/Algorithms & Data Structures]]
