package CD19;

import java.io.PrintWriter;

public class OutputController {

    ListingFile listingFile;

    public OutputController(ListingFile listingFile) {
        this.listingFile = listingFile;
    }

    public void printToOutput(PrintWriter printWriter){
        /*
        Re-produce the input which has been scanned as a separate listing file.
        Add line numbers to the start of each line
        It can also output errors associated with any line after the line has been produced on the listing
        * Print to .lst and terminal
        * */
    }
}
